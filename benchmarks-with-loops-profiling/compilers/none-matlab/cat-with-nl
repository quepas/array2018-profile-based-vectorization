#!/usr/bin/env python
import sys

if __name__ == "__main__":
    content = []
    for f in sys.argv[1:]:
        content.append(open(f).read())
    sys.stdout.write("\n".join(content) + "\n")
        
    
